apiVersion: v1
kind: Service
metadata:
  name: notifications-api
  namespace: notifications
spec:
  selector:
    app: notifications-api
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
